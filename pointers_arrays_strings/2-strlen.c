#include "main.h"
#include <string.h>

/**
 * _strlen - check the code;
 * @s: Take a string as value;
 * Return: Always 0;
 */

int _strlen(const char *str)
{
	size_t length = strlen(str);
	return (length);
}
