#include "main.h"

/**
 * add - check the code;
 * @a: take an integer value;
 * @b: take another integer value;
 * Return: Always 0 (if success);
 */

int add(int a, int b)

{
	int c = a + b;

	return (c);

}
