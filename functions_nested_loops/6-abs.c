#include "main.h"

/**
 * _abs - check the code;
 * @N: take an integeras a value;
 * Return: Always 0;
 */

int _abs(int N)
{
	if (N < 0)
	{
		N = (-1) * N;
		return (N);
	}
	else
	{
		return (N);
	}
	return (0);
}
