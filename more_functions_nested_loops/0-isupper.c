#include "main.h"

/**
 * _isupper - check the code.
 *@c: take a value
 * Return: Always 0 if it is Lowercase;
 */

int _isupper(char c)

{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}

	else
	{
		return (0);
	}
	return (0);
}
