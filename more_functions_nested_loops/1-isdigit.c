#include "main.h"

/**
 * _isdigit - check the code.
 *@c: take an integer value
 * Return: Always 1 if it is a Digit:
 */

int _isdigit(char c)

{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}

	else
	{
		return (0);
	}
	return (0);
}
